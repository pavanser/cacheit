declare namespace Enums {
  export enum Actions {
    add = 'added',
    clear = 'cleared',
    delete = 'deleted',
    update = 'updated',
    upsert = 'upserted',
  }

  export enum Statuses {
    success = 'success',
    failed = 'failed',
  }
}